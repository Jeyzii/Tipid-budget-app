# Leave empty during local dev so requests go through Vite’s proxy (same origin → no CORS issues).
# Only set this if the API is on a different host than the Vite dev server, e.g. https://api.example.com
VITE_API_BASE_URL=
